{
  "name": "firestore",
  "version": "1.0.0",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"jest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.1",
    "firebase": "^9.1.0",
    "jest": "^27.4.0"
  }
}
